## 1. AppShell Layout

- [x] 1.1 In `src/components/AppShell.tsx`, replace the two-section topbar row (left `w-[340px]` div and right `flex-1` div) with a single full-width `div` using `bg-background`, keeping the `h-[44px]` height and `border-b border-border`
